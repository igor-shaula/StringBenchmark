<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.igor.shaula.string_benchmark">

    <application
        android:name=".job_core.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- we really need large heap here -->

        <activity
            android:name=".screens.mandatory.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <service
            android:name=".job_core.TestingIntentService"
            android:exported="false">
        </service>

        <activity
            android:name=".screens.additional.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".screens.mandatory.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.igor.shaula.string_benchmark.screens.mandatory.MainActivity"/>
        </activity>
    </application>

</manifest>